<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>{{title}} - MNGo</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
  <style>
    :root {
      --color-primary: #FF8C00;
      --color-primary-hover: #e67e00;
    }

    #mainNav .nav-pills .nav-link {
      width: 110px;
      text-align: center;
      font-weight: 500;
      color: #495057;
      transition: all 0.2s ease;
      border-radius: 25px;
      padding: 8px 16px;
    }

    #mainNav .nav-pills .nav-link:hover {
      color: var(--color-primary);
      background-color: rgba(255, 140, 0, 0.1);
    }

    #mainNav .nav-pills .nav-link.active {
      background-color: var(--color-primary);
      font-weight: 700;
      color: white;
      box-shadow: 0 4px 12px rgba(255, 140, 0, 0.35);
    }

    .btn-orange {
      background-color: var(--color-primary);
      color: white;
      border: none;
    }

    .btn-orange:hover {
      background-color: var(--color-primary-hover);
      color: white;
    }

    /* Custom Pagination Styles */
    .pagination {
      gap: 5px;
    }

    .pagination .page-item .page-link {
      border: none;
      border-radius: 8px;
      padding: 8px 14px;
      color: #6c757d;
      font-weight: 600;
      background-color: #f8f9fa;
      transition: all 0.2s ease;
    }

    .pagination .page-item .page-link:hover {
      background-color: var(--color-primary);
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(255, 140, 0, 0.3);
    }

    .pagination .page-item.active .page-link {
      background-color: var(--color-primary);
      color: white;
      box-shadow: 0 4px 12px rgba(255, 140, 0, 0.4);
    }

    .pagination .page-item.disabled .page-link {
      background-color: #e9ecef;
      color: #adb5bd;
      cursor: not-allowed;
    }

    .pagination .page-item:first-child .page-link,
    .pagination .page-item:last-child .page-link {
      border-radius: 8px;
    }

    html {
      overflow-y: scroll;
    }
  </style>
  {{{_sections.css}}}
</head>

<body class="bg-light-subtle">

  <nav class="navbar navbar-expand-lg bg-white shadow-sm">
    <div class="container-fluid">
      <!-- Account layout change: removed px-3 for full width navbar -->
      <a class="navbar-brand" href="/">
        <span style="color: #FF8C00; font-weight: 800;">MN</span><span
          style="color: #212529; font-weight: 800;">Go</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="mainNav">

        <div class="d-none d-lg-block" style="width: 200px;"></div>

        <ul class="nav nav-pills mx-auto">
          <li class="nav-item"><a class="nav-link {{#if (eq activeNav 'Home')}}active{{/if}}" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link {{#if (eq activeNav 'Menu')}}active{{/if}}" href="/menu">Menu</a></li>
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Watchlist')}}active{{/if}}" href="/profile/watchlist">Watchlist</a>
          </li> 
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Account')}}active{{/if}}" href="/profile">Account</a>
          </li>
        </ul>

        <div class="d-flex justify-content-end" style="width: 200px;">
          {{#if isAuthenticated}}
          <div class="d-flex align-items-center">
            <div class="text-end me-2">
              <div class="fw-bold text-dark" style="font-size: 1.1rem;">{{authUser.full_name}}</div>
              <div class="d-flex align-items-center justify-content-end" style="font-size: 0.9rem;">
                <span class="text-muted me-2">Points: {{authUser.positive_point}}/{{authUser.points}}</span>
                {{#if (eq authUser.role 1)}}
                <span class="badge rounded-pill bg-success">Seller</span>
                {{else}}
                <span class="badge rounded-pill bg-success">Bidder</span>
                {{/if}}
              </div>
            </div>
            <div class="rounded-circle d-flex align-items-center justify-content-center fw-bold text-dark"
              style="width: 45px; height: 45px; background-color: #d9d9d9; font-size: 1.5rem;">
              {{#if authUser.full_name}}{{authUser.full_name.[0]}}{{else}}U{{/if}}
            </div>
          </div>
          {{else}}
          <a class="btn btn-orange rounded-pill" href="/signin">Sign In</a>
          {{/if}}
        </div>

      </div>
    </div>
  </nav>

  <main class="container my-4 bg-white p-4 shadow-sm" style="min-height: 70vh;">
    {{{body}}}
  </main>

  <footer class="text-center text-muted p-4">
    © 2025 MNGo - Web Development Final Project (Minh and Nam)
  </footer>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  {{{_sections.js}}}
</body>

</html>