<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <title>{{title}} - MNGo</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  {{{_sections.css}}}
</head>
<body class="bg-light-subtle"> 

  <nav class="navbar navbar-expand-lg bg-white shadow-sm">
    <div class="container-fluid">
      
      <a class="navbar-brand" href="/">
        <span style="color: #FF8C00; font-weight: 800;">MN</span><span style="color: #212529; font-weight: 800;">Go</span>
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="mainNav">
        <ul class="nav nav-pills mx-auto">
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Home')}}active{{/if}}" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Menu')}}active{{/if}}" href="#">Menu</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Watchlist')}}active{{/if}}" href="#">Watchlist</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Account')}}active{{/if}}" href="/account/profile">Account</a>
          </li>
        </ul>

        <div class="d-flex">
          {{#if isAuthenticated}}
            <div class="dropdown">
              <button class="btn btn-outline-secondary dropdown-toggle rounded-pill" type="button" data-bs-toggle="dropdown">
                Hi, {{authUser.full_name}}
              </button>
              <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" href="/dashboard">Dashboard</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="/accounts/signout">Sign Out</a></li>
              </ul>
            </div>
          {{else}}
            <a class="btn btn-danger rounded-pill" href="/account/signin">Sign In</a>
          {{/if}}
        </div>

      </div>
    </div>
  </nav>

  <div class="bg-light border-bottom">
    <div class="container py-2">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb mb-0">
          <li class="breadcrumb-item active" aria-current="page">{{title}}</li>
        </ol>
      </nav>
    </div>
  </div>

  <main class="container my-4 bg-white p-4 shadow-sm" style="min-height: 70vh;">
    {{{body}}}
  </main>

  <footer class="text-center text-muted p-4">
    © 2025 MNGo - Web Development Final Project (Minh and Nam)
  </footer>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  {{{_sections.js}}}
</body>
</html>