<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - MNGo Checkout</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
  
  <link href="https://releases.transloadit.com/uppy/v3.23.0/uppy.min.css" rel="stylesheet">

  <style>
    :root {
      --color-purple: #6f42c1;
      --color-purple-light: #e0cffc;
      --color-purple-bg: #f3f0ff;
    }

    body {
      background-color: var(--color-purple-bg);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Header tối giản */
    .checkout-header {
      background: white;
      padding: 15px 0;
      box-shadow: 0 2px 10px rgba(111, 66, 193, 0.1);
    }

    .brand-logo {
      font-size: 1.8rem;
      text-decoration: none;
      font-weight: 800;
    }

    /* Hiệu ứng bồng bềnh cho Card chính */
    .wizard-card {
      background: white;
      border-radius: 20px;
      border: none;
      box-shadow: 0 10px 30px rgba(111, 66, 193, 0.15);
      padding: 40px;
      margin-top: 30px;
      margin-bottom: 50px;
      animation: floatUp 0.8s ease-out;
      transition: all 0.3s ease;
    }

    @keyframes floatUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Timeline / Stepper Styles */
    .stepper-wrapper {
      margin-top: auto;
      display: flex;
      justify-content: space-between;
      margin-bottom: 40px;
      position: relative;
    }

    .stepper-wrapper::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 0;
      transform: translateY(-50%);
      width: 100%;
      height: 4px;
      background-color: #e9ecef;
      z-index: 0;
    }

    .stepper-item {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      flex: 1;
    }

    .step-counter {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background-color: #fff;
      border: 3px solid #e9ecef;
      display: flex;
      justify-content: center;
      align-items: center;
      font-weight: bold;
      color: #adb5bd;
      margin-bottom: 10px;
      transition: all 0.3s;
    }

    /* Active Step */
    .stepper-item.active .step-counter {
      border-color: var(--color-purple);
      background-color: var(--color-purple);
      color: #fff;
      box-shadow: 0 0 0 5px var(--color-purple-light);
    }

    .stepper-item.active .step-name {
      color: var(--color-purple);
      font-weight: 700;
    }

    /* Completed Step */
    .stepper-item.completed .step-counter {
      border-color: var(--color-purple);
      background-color: var(--color-purple);
      color: #fff;
    }

    .stepper-item.completed::after {
      content: "";
      position: absolute;
      width: 100%;
      height: 4px;
      background-color: var(--color-purple);
      top: 20px; /* Half of counter height */
      left: 50%;
      z-index: -1;
    }
    
    .stepper-item:last-child::after {
      content: none;
    }

    .step-name {
      font-size: 0.9rem;
      color: #6c757d;
      font-weight: 500;
    }

    /* Buttons */
    .btn-purple {
      background-color: var(--color-purple);
      color: white;
      border-radius: 50px;
      padding: 12px 30px;
      font-weight: 600;
      border: none;
      transition: all 0.3s;
    }

    .btn-purple:hover {
      background-color: #5a32a3;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(111, 66, 193, 0.3);
      color: white;
    }

    /* Waiting State Animation */
    .waiting-pulse {
      width: 80px;
      height: 80px;
      background-color: var(--color-purple-light);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      animation: pulse 2s infinite;
    }
    
    @keyframes pulse {
      0% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(111, 66, 193, 0.7); }
      70% { transform: scale(1); box-shadow: 0 0 0 20px rgba(111, 66, 193, 0); }
      100% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(111, 66, 193, 0); }
    }

  </style>
  {{{_sections.css}}}
</head>

<body>

  <header class="checkout-header">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <a class="brand-logo" href="/">
          <span style="color: #FF8C00;">MN</span><span style="color: var(--color-purple);">Go</span> <span style="font-size: 1rem; color: #6c757d; font-weight: normal;">| Checkout</span>
        </a>
        <a href="/profile" class="btn btn-outline-secondary rounded-pill btn-sm">Exit</a>
      </div>
    </div>
  </header>

  <div class="container">
    {{{body}}}
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  {{{_sections.js}}}
</body>

</html>