<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{{title}} - MNGo</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">

    <style>
        :root {
            --accent: #ff8c00;
        }

        .admin-sidebar {
            position: sticky;
            top: 1rem;
            border: 1px solid #f0f0f0;
            border-radius: 12px;
            background: #fff;
        }

        .admin-sidebar .list-group-item.active {
            background-color: rgba(255, 140, 0, 0.12);
            color: #b35f00;
            border-color: rgba(255, 140, 0, 0.22);
            font-weight: 700;
        }

        .admin-sidebar .list-group-item:hover {
            background: #fff6eb;
        }
    </style>

    {{{_sections.css}}}
</head>

<body class="bg-light-subtle">

    <nav class="navbar navbar-expand-lg bg-white shadow-sm">
        <div class="container-fluid">

            <a class="navbar-brand" href="/">
                <span style="color: #FF8C00; font-weight: 800;">MN</span><span
                    style="color: #212529; font-weight: 800;">Go</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                <span class="navbar-toggler-icon"></span>
            </button>


            <div class="collapse navbar-collapse" id="mainNav">
                
                <div class="d-flex ms-auto">
                    {{#if isAuthenticated}}
                    <div class="d-flex align-items-center">
                        <div class="text-end me-2">
                            <div class="fw-bold" style="font-size: 1.1rem; color: #000;">
                                {{authUser.full_name}}
                            </div>

                            <div class="d-flex align-items-center justify-content-end" style="font-size: 0.9rem;">
                                <span class="badge rounded-pill bg-success">Admin</span>
                            </div>
                        </div>

                        <div class="rounded-circle d-flex align-items-center justify-content-center fw-bold text-dark"
                            style="width: 45px; height: 45px; background-color: #d9d9d9; font-size: 1.5rem;">
                            {{#if authUser.full_name}}{{authUser.full_name.[0]}}{{else}}U{{/if}}
                        </div>
                    </div>
                    {{else}}
                    <a class="btn btn-danger rounded-pill" href="/account/signin">Sign In</a>
                    {{/if}}
                </div>

            </div>
        </div>
    </nav>

    <div class="container my-4" style="min-height: 70vh;">
        <div class="row g-3">
            <aside class="col-12 col-md-3">
                <div class="admin-sidebar p-3 shadow-sm">
                    <div class="d-flex align-items-center justify-content-between mb-2">
                        <div class="fw-bold">Admin</div>
                        <span class="badge text-bg-light border">MNGo</span>
                    </div>
                    <div class="list-group list-group-flush">
                        <a href="/admin/categories"
                            class="list-group-item list-group-item-action {{#if (eq activeAdmin 'categories')}}active{{/if}}">
                            <i class="bi bi-folder me-2"></i>Categories
                        </a>
                        <a href="/admin/products"
                            class="list-group-item list-group-item-action {{#if (eq activeAdmin 'products')}}active{{/if}}">
                            <i class="bi bi-box-seam me-2"></i>Products
                        </a>
                        <a href="/admin/upgrade-requests"
                            class="list-group-item list-group-item-action {{#if (eq activeAdmin 'upgrade')}}active{{/if}}">
                            <i class="bi bi-shield-plus me-2"></i>Upgrade Requests
                        </a>
                    </div>
                </div>
            </aside>
            <main class="col-12 col-md-9">
                <div class="bg-white p-4 shadow-sm rounded-3">
                    {{{body}}}
                </div>
            </main>
        </div>
    </div>

    <footer class="text-center text-muted p-4">
        © 2025 MNGo - Web Development Final Project (Minh and Nam)
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    {{{_sections.js}}}
</body>

</html>