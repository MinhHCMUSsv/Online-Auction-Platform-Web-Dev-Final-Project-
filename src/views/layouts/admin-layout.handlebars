<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{{title}} - MNGo Admin</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">

    <style>
        :root {
            --accent: #ff8c00; /* Main Orange Color */
            --accent-light: #fff6eb; /* Light background for active/hover */
            --accent-dark-text: #b35f00; /* Darker text for active element */
            --navbar-height: 70px;
        }

        body {
            background-color: #f4f6f9; /* Soft background color */
        }

        /* --- Navbar Styles --- */
        .navbar {
            height: var(--navbar-height);
            border-bottom: 1px solid #e9ecef;
        }

        .navbar-brand .logo-accent {
            color: var(--accent); 
            font-weight: 800;
        }

        .navbar-brand .logo-dark {
            color: #212529; 
            font-weight: 800;
        }

        .profile-badge {
            font-size: 0.9rem;
            background-color: rgba(31, 154, 84, 0.12); /* Success green background */
            color: #1f9a54; /* Success green text */
            font-weight: 600;
        }

        .avatar-placeholder {
            width: 45px; 
            height: 45px; 
            background-color: #e9ecef; /* Lighter background for avatar */
            color: #6c757d;
            font-size: 1.5rem;
            font-weight: 700;
            border: 2px solid #ced4da;
        }

        /* --- Admin Sidebar Styles --- */
        .admin-sidebar {
            position: fixed;
            top: calc(var(--navbar-height) + 1.5rem);
            left: 1rem;
            width: 280px;
            height: calc(100vh - var(--navbar-height) - 3rem);
            overflow-y: auto;
            border: 1px solid #e0e0e0;
            border-radius: 12px;
            background: #fff;
            padding: 0 !important; /* Remove card padding for flush list group */
            z-index: 100;
        }

        @media (max-width: 767px) {
            .admin-sidebar {
                position: sticky;
                top: 1.5rem;
                width: 100%;
                height: auto;
                left: 0;
            }
        }
        
        /* Main content adjustment for fixed sidebar */
        .admin-main-content {
            margin-left: calc(280px + 2rem);
            padding-right: 1rem;
        }

        @media (max-width: 767px) {
            .admin-main-content {
                margin-left: 0;
                padding-right: 0;
            }
        }
        
        .sidebar-header {
            padding: 1rem 1.25rem;
            border-bottom: 1px solid #f0f0f0;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: #6c757d;
            font-weight: 600;
        }

        .admin-sidebar .list-group-item {
            padding: 0.85rem 1.25rem;
            color: #495057;
            font-weight: 500;
            transition: all 0.2s;
            border: none;
        }

        .admin-sidebar .list-group-item.active {
            background-color: var(--accent-light);
            color: var(--accent-dark-text); 
            /* border-left: 4px solid var(--accent); */ /* Optional accent bar */
            font-weight: 700;
            box-shadow: inset 3px 0 0 var(--accent); /* Visual indicator for active state */
        }

        .admin-sidebar .list-group-item:hover {
            background: var(--accent-light);
            color: #212529;
        }

        /* --- Main Content Area --- */
        main {
            padding-top: 0.5rem; /* Little gap from the top */
        }

        main > div {
            padding: 1.5rem !important; /* Increase padding for main content */
            border-radius: 12px !important;
        }

        /* Sidebar scrollbar styling */
        .admin-sidebar::-webkit-scrollbar {
            width: 6px;
        }

        .admin-sidebar::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 10px;
        }

        .admin-sidebar::-webkit-scrollbar-thumb {
            background: #cbd5e0;
            border-radius: 10px;
        }

        .admin-sidebar::-webkit-scrollbar-thumb:hover {
            background: #a0aec0;
        }
        
    </style>

    {{{_sections.css}}}
</head>

<body>

    <nav class="navbar navbar-expand-lg bg-white shadow-sm sticky-top">
        <div class="container-fluid"> <a class="navbar-brand" href="/">
                <span class="logo-accent">MN</span><span class="logo-dark">Go</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                <span class="navbar-toggler-icon"></span>
            </button>


            <div class="collapse navbar-collapse" id="mainNav">
                
                <div class="d-flex ms-auto">
                    {{#if isAuthenticated}}
                    <div class="dropdown">
                        <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="text-end me-2 d-none d-lg-block">
                                <div class="fw-bold" style="font-size: 1.1rem; color: #000;">
                                    {{authUser.full_name}}
                                </div>
                                <div class="d-flex align-items-center justify-content-end" style="font-size: 0.9rem;">
                                    <span class="badge profile-badge">Administrator</span>
                                </div>
                            </div>
    
                            <div class="rounded-circle avatar-placeholder d-flex align-items-center justify-content-center fw-bold text-dark flex-shrink-0">
                                {{#if authUser.full_name}}{{authUser.full_name.[0]}}{{else}}U{{/if}}
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2">
                            <li><h6 class="dropdown-header">{{authUser.full_name}}</h6></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/admin">Dashboard</a></li>
                            <li><a class="dropdown-item" href="/admin/settings">Settings</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/signout">Sign Out</a></li>
                          </ul>
                    </div>
                    {{else}}
                    <a class="btn btn-warning" href="/signin">Sign In</a>
                    {{/if}}
                </div>

            </div>
        </div>
    </nav>

    <div class="container-fluid" style="min-height: 100px;">
        <div class="row g-4"> 
            <aside class="col-12 col-md-3">
                <div class="admin-sidebar shadow-sm">
                    <div class="sidebar-header d-flex align-items-center justify-content-between">
                        ADMIN NAVIGATION
                    </div>
                    <div class="list-group list-group-flush">
                        
                        <a href="/admin/users"
                            class="list-group-item list-group-item-action {{#if (eq activeAdmin 'users')}}active{{/if}}">
                            <i class="bi bi-people me-2"></i>User Management
                        </a>
                        <a href="/admin/categories"
                            class="list-group-item list-group-item-action {{#if (eq activeAdmin 'categories')}}active{{/if}}">
                            <i class="bi bi-folder me-2"></i>Categories
                        </a>
            
                        <div class="border-top mx-3 my-2"></div>
                        
                        <a href="/admin/settings"
                            class="list-group-item list-group-item-action {{#if (eq activeAdmin 'settings')}}active{{/if}}">
                            <i class="bi bi-gear me-2"></i>Settings
                        </a>
                        
                    </div>
                </div>
            </aside>
            <main class="col-12 col-md-9 admin-main-content mb-5">
                <div class="bg-white shadow-sm">
                    {{{body}}}
                </div>
            </main>
        </div>
    </div>

    <footer class="text-center text-muted p-4 border-top mt-7">
        <div class="container-xl">
            © 2025 MNGo | Built for Web Development Final Project
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    {{{_sections.js}}}
</body>

</html>