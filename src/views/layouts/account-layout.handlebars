<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>{{title}} - MNGo</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">

  <style>
    :root {
      --color-orange-bg: rgba(255, 140, 0, 0.6);
      --color-orange-text: #FF8C00;
      --color-green: #198754;
      --color-red: #DC3545;
      --color-blue: #0D6EFD;
    }

    /* Banner */
    .account-banner {
      background-color: var(--color-orange-bg);
      height: 180px;
      display: flex;
      align-items: flex-end;
      padding-bottom: 20px;
    }

    .account-banner h1 {
      font-weight: 800;
      color: white;
      text-transform: uppercase;
      font-size: 3rem;
      margin: 0;
    }

    /* Sidebar */
    .avatar-circle {
      width: 80px;
      height: 80px;
      background-color: #e9ecef;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 2.5rem;
      font-weight: bold;
      color: #495057;
    }

    .seller-badge {
      background-color: var(--color-green);
      color: white;
      padding: 3px 12px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .bidder-badge {
      background-color: #198754;
      color: white;
      padding: 3px 12px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .logout-link {
      color: var(--color-red);
      font-weight: 700;
      text-decoration: none;
      font-size: 1.1rem;
    }

    /* Nav */
    .profile-nav .nav-link {
      color: #212529;
      font-weight: 600;
      padding: 12px 0;
      font-size: 1.1rem;
    }

    .profile-nav .nav-link:hover {
      color: var(--color-blue);
    }

    .profile-nav .nav-link.active-custom {
      color: var(--color-blue);
      font-weight: 800;
    }

    .btn-upgrade {
      background-color: var(--color-green);
      color: white;
      font-weight: 700;
      padding: 10px 20px;
      border-radius: 20px;
      border: none;
      text-decoration: none;
      display: inline-block;
      margin-top: 15px;
    }

    #mainNav .nav-pills .nav-link {
      width: 110px;
      text-align: center;
    }

    html {
      overflow-y: scroll;
    }
  </style>

  {{{_sections.css}}}
</head>

<body class="bg-light-subtle">

  <nav class="navbar navbar-expand-lg bg-white shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <span style="color: #FF8C00; font-weight: 800;">MN</span><span
          style="color: #212529; font-weight: 800;">Go</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="mainNav">

        <div class="d-none d-lg-block" style="width: 200px;"></div>

        <ul class="nav nav-pills mx-auto">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/products">Menu</a></li>
          <li class="nav-item">
            <a class="nav-link {{#if (eq activeNav 'Watchlist')}}active{{/if}}"
              href="/account/profile/watchlist">Watchlist</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#unless (eq activeNav 'Watchlist')}}active{{/unless}}"
              href="/account/profile">Account</a>
          </li>
        </ul>

        <div class="d-flex justify-content-end" style="width: 200px;">
          {{#if isAuthenticated}}
          <div class="d-flex align-items-center">
            <div class="text-end me-2">
              <div class="fw-bold text-dark" style="font-size: 1.1rem;">{{authUser.full_name}}</div>
              <div class="d-flex align-items-center justify-content-end" style="font-size: 0.9rem;">
                <span class="text-muted me-2">Points: {{authUser.points}}</span>
                {{#if (eq authUser.role 1)}}
                <span class="badge rounded-pill bg-success">Seller</span>
                {{else}}
                <span class="badge rounded-pill bg-success">Bidder</span>
                {{/if}}
              </div>
            </div>
            <div class="rounded-circle d-flex align-items-center justify-content-center fw-bold text-dark"
              style="width: 45px; height: 45px; background-color: #d9d9d9; font-size: 1.5rem;">
              {{#if authUser.full_name}}{{authUser.full_name.[0]}}{{else}}U{{/if}}
            </div>
          </div>
          {{else}}
          <a class="btn btn-danger rounded-pill" href="/account/signin">Sign In</a>
          {{/if}}
        </div>

      </div>
    </div>
  </nav>

  <div class="container-fluid p-0 mb-5">
    <div class="account-banner">
      <div class="container">
        <h1>My Account</h1>
      </div>
    </div>
  </div>

  <div class="container mb-5">
    <div class="row">

      <div class="col-md-3">
        <div class="d-flex align-items-center mb-4">
          <div class="avatar-circle me-3">{{#if authUser.full_name}}{{authUser.full_name.[0]}}{{else}}U{{/if}}</div>
          <div>
            <h4 class="mb-1 fw-bold">{{authUser.full_name}}</h4>
            <div class="mb-2">
              {{#if (eq authUser.role 1)}}
              <span class="seller-badge">Seller</span>
              {{else}}
              <span class="bidder-badge">Bidder</span>
              {{/if}}
              <span class="text-muted ms-1 fw-bold">Points: {{authUser.points}}</span>
            </div>
            <a href="javascript: document.querySelector('#frmSignOutLayout').submit();" class="logout-link">Logout</a>
          </div>
          <form method="POST" action="/account/signout" id="frmSignOutLayout"></form>
        </div>

        <nav class="nav flex-column profile-nav">
          {{#if (eq authUser.role 1)}}
          <a class="nav-link {{#if (eq activeNav 'CreateAuction')}}active-custom{{/if}}" href="/seller/create">Create
            Auction</a>
          <a class="nav-link" href="#">Active Auctions</a>
          <a class="nav-link" href="#">Finished auctions</a>
          <hr class="my-3 text-secondary">
          {{/if}}

          <a class="nav-link {{#if (eq activeNav 'AccountSettings')}}active-custom{{/if}}"
            href="/account/profile">Account Settings</a>
          <a class="nav-link" href="#">My Ratings</a>

          <hr class="my-3 text-secondary">

          <a class="nav-link {{#if (eq activeNav 'Watchlist')}}active-custom{{/if}}"
            href="/account/profile/watchlist">Watchlist</a>
          <a class="nav-link {{#if (eq activeNav 'ActiveBids')}}active-custom{{/if}}"
            href="/account/profile/active">Active Bids</a>
          <a class="nav-link {{#if (eq activeNav 'WonItems')}}active-custom{{/if}}" href="/account/profile/won">Won
            items</a>

          {{#unless (eq authUser.role 1)}}
          <div class="mt-3">
            <a href="#" class="btn-upgrade">Upgrade to seller</a>
          </div>
          {{/unless}}
        </nav>
      </div>

      <div class="col-md-9">
        {{{body}}}
      </div>

    </div>
  </div>

  <footer class="text-center text-muted p-4">
    © 2025 MNGo - Web Development Final Project (Minh and Nam)
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  {{{_sections.js}}}
</body>

</html>